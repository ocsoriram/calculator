## 開発環境起動
```bash
./startDev.sh 
```

## React起動コマンド

```bash
 npm run dev:frontend
```


## fastAPI起動コマンド(開発環境)

```bash
uvicorn backend.main:app --reload
```

両方のサーバをルートから叩ける。
プロジェクトの方針として、絶対パスでimportをするため、USGIサーバはプロジェクトルートで起動すること。

## APIドキュメントの参照（Swagger UI）

http://127.0.0.1:8000/docs


## クエリパラメータ基本

https://example.com/?keyword=AI&page=2

?: パラメータの開始.  
keyword=AI: 検索キーワード「AI」を指定するパラメータ.  
&: パラメータの区切り.  
page=2: 2ページ目を指定するパラメータ.  


## reactのテスト実行

### テスト実行
```bash
npm run -w frontend test
```

### 監視モードで実行
```bash
npm run -w frontend test -- --watch
```

### UIモード
```bash
npm run -w frontend test -- --ui
```

### テスト名指定実行
```bash
npm run -w frontend test HistoryList.test.tsx 
```

## TODO
### フロントエンド実装
- ~~計算結果を丸めて表示する機能を実装する。→backendからはfloatで渡されるため ~~
- ~~()の閉じ忘れをformエラーで返す。→補完機能？~~
- ~~計算結果が下12桁まで表示れるので修正する~~
- ~~一つ前の計算式のみ上部に表示されるように修正する~~
- ~~=が押されるまでは、計算式をdivに表示しない~~
- ~~=のあとに数字ボタンが押された時は、新しい式として計算を始める~~
- ~~=のあとに演算ボタンが押された時は、前回の答えに対して演算する~~
- ~~数字と演算子のボタンの色を変える~~
- ~~計算機部分の表示位置を固定する~~
- ~~式の先頭で演算子を押せないようにする () 　以外~~
- ~~バックエンドでエラーが出たときのメッセージ表示を細かく設定する~~
- ~~不正文字(日本語や英文字)を入力不可能にする~~
- ~~12 34 が通ってしまう。数値同士の間のスペースは通さないようにする~~
- ~~デバッグの構成~~
- ~~レスポンシブ対応~~
- ~~日本語入力時に、エンターでqueryを投げている~~
- ~~任意の履歴のみ削除する機能~~
- ~~一度エラーになると=ボタンの非活性が外れない　FIXME~~
- CSSを調整する
- テストの追加
- ブラウザの履歴保存機能を有効にする
- 演算子を続けて入力できてしまうエラー。クリティカルではないがUXの向上に必要。
- 計算結果から続けて入力する値が演算子? そのまま式を続ける: 初期化して値を入力する
- buttonのborderのCSSが微妙な動きをしている index.cssとtailwindが競合している index.cssを削除してtailwindに一本化すれば治るはず
- クリアボタン 押下時のアニメーション追加

### バックエンド実装
- ~~CORSの設定~~
- ~~POST /calc HTTP/1.1" 307 Temporary Redirect が吐かれる原因調査 ルート定義から"/"を削除した~~
